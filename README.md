# CommanderBiz Kali Linux (ARM64)

A highly optimized, crash-proof build of Kali Linux for Android (Termux).
Built from source to bypass common PRoot errors, with pre-patched Browser and GUI.

## 🚀 Quick Install (The One-Liner)

Copy and paste this into Termux:

```bash
pkg install wget -y && pkg rm -rf install.sh kali-fs && wget [https://raw.githubusercontent.com/commanderbiz/andronix-lab/main/install.sh](https://raw.githubusercontent.com/commanderbiz/andronix-lab/main/install.sh) && bash install.sh
